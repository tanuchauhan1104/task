# In production on Railway, the frontend is served by the same backend server
# so the API url is just '' (empty = same origin)
# For local development, the proxy in package.json handles it
REACT_APP_API_URL=
